from setuptools import setup

setup(
   name='pyDenStream',
   version='0.1',
   description='TBC',
   author='TBC',
   author_email='TBC',
   packages=['pyDenStream'],  #same as name
   install_requires=['numpy', 'numba'], #external packages as dependencies
)
